apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-prova.fullname" . }}
  labels:
    {{- include "devops-prova.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-prova.selectorLabels" . | nindent 4 }}
